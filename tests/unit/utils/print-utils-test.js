import printUtils from 'restaurent-app/utils/print-utils';
import { module, test } from 'qunit';

module('Unit | Utility | print utils');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = printUtils();
  assert.ok(result);
});
