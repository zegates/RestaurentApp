import { utilMultiply } from 'restaurent-app/helpers/util-multiply';
import { module, test } from 'qunit';

module('Unit | Helper | util multiply');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = utilMultiply([42]);
  assert.ok(result);
});
